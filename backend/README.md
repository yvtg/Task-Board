
`server.js` -> server đơn giản (ko express)
```
npm init -y

node server.js -> chay server
```

cap nhat packet 
```
npm install express -> framework express
npm install nodemon -> moi khi code change, ko can chay lai
npm install axios -> frontend
```

-> tao file server.js 
-> tao router, listen port

add todo
edit  
delete
mark to as complete

schema
```json
{
    id: number
    title: string
    completed: boolean
}
```