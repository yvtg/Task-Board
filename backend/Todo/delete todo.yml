info:
  name: delete todo
  type: http

http:
  method: DELETE
  url: http://localhost:3000/api/todos/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
