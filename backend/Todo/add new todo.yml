info:
  name: add new todo
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/todos
  body:
    type: json
    data: |-
      {
        "title": "Hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
