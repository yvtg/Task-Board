info:
  name: edit todo
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:3000/api/todos/1
  body:
    type: json
    data: |-
      {
        "title": "zzz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​